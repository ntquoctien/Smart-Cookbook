import type { UserProfile } from '../types/user';

export const defaultUserProfile: UserProfile = {
  name: 'Kitchen Explorer',
  email: 'chef@smartcookbook.ai',
  cookingLevel: 'Beginner-friendly',
  dietPreferences: ['Healthy focus'],
};
